name: Test out publish call

on:
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

  publish:
    runs-on: ubuntu-latest
    needs: fetch
    uses: npm/statusboard/.github/workflows/publish.yml@main